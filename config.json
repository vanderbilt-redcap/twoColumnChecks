{
	"name": "Two Column Checks",

	"namespace": "Vanderbilt\\TwoColumnChecksExternalModule",

	"authors": [
		{
			"name": "DataCore",
			"email": "datacore@vumc.org",
			"institution": "Vanderbilt University Medical Center"
		}
	],

	"description": "Reorients a left-vertically-aligned group of checkboxes into two columns - only if more than ten checkboxes exist in the list (surveys only). This will save room on a survey, but note that it requires the checkbox have LV alignment.",

	"framework-version": 14,
	
	"compatibility": {
		"php-version-min": "5.5.0",
		"php-version-max": "",
		"redcap-version-min": "8.0.0",
		"redcap-version-max": ""
	}
}
